"""loci's two memory layers.

  session.py  — ephemeral, recency-shaped rolling transcript (NOT OKF).
  okf.py      — durable, structured knowledge in Open Knowledge Format v0.1.

Do not conflate them: the session is what was just said; the knowledge bundle is
what loci durably knows.
"""
